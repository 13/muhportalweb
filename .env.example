# MQTT Broker Configuration (server-side, non-WebSocket)
MQTT_URL=mqtt://192.168.22.5:1883

# Socket.IO URL (optional - defaults to current host)
# WS_URL=http://localhost:3000

# CORS Origin (optional - defaults to * for all origins, set to specific domain in production)
# CORS_ORIGIN=https://yourdomain.com

# Nuxt Configuration
NUXT_HOST=0.0.0.0
NUXT_PORT=3000
