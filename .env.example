# MQTT WebSocket Configuration (used by browser)
# Use ws:// for HTTP or wss:// for HTTPS (required to avoid mixed content errors)
MQTT_WS_URL=ws://192.168.22.5:1884
# For HTTPS with nginx-proxy, use secure WebSocket:
# MQTT_WS_URL=wss://mqtt.yourdomain.com:8884

# Nuxt Configuration
NUXT_HOST=0.0.0.0
NUXT_PORT=3000
